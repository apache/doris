SELECT cast(v:repo.name as string), count() AS stars FROM github_events WHERE cast(v:type as string) = 'WatchEvent' AND year(cast(v:created_at as datetime)) = '2020' GROUP BY cast(v:repo.name as string) ORDER BY stars DESC LIMIT 50

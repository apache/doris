# Manual

You should promise the work directory of script is on the path: regression-test/suites/tpcds_sf1

And execute following command:

```
 python3 script/run.py ${YOUR_DORIS_HOME}/regression-test/suites/tpch_sf1/script/task.batch
```

Add the sql you want to run in the task.batch file.

The first column represents the SQL file, second column is the groovy file to be generated, third is the number of

TPC-H SQL.

```
sql/q01.sql explain/test_q1.groovy 1
```

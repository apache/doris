<?xml version="1.0" encoding="UTF-8"?>
<!--
Licensed to the Apache Software Foundation (ASF) under one
or more contributor license agreements.  See the NOTICE file
distributed with this work for additional information
regarding copyright ownership.  The ASF licenses this file
to you under the Apache License, Version 2.0 (the
"License"); you may not use this file except in compliance
with the License.  You may obtain a copy of the License at

  http://www.apache.org/licenses/LICENSE-2.0

Unless required by applicable law or agreed to in writing,
software distributed under the License is distributed on an
"AS IS" BASIS, WITHOUT WARRANTIES OR CONDITIONS OF ANY
KIND, either express or implied.  See the License for the
specific language governing permissions and limitations
under the License.
-->
<project xmlns="http://maven.apache.org/POM/4.0.0"
         xmlns:xsi="http://www.w3.org/2001/XMLSchema-instance"
         xsi:schemaLocation="http://maven.apache.org/POM/4.0.0 http://maven.apache.org/xsd/maven-4.0.0.xsd">
    <modelVersion>4.0.0</modelVersion>

    <parent>
        <groupId>org.apache.doris</groupId>
        <artifactId>fe-connector</artifactId>
        <version>${revision}</version>
        <relativePath>../pom.xml</relativePath>
    </parent>

    <artifactId>fe-connector-iceberg</artifactId>
    <packaging>jar</packaging>
    <name>Doris FE Connector - Iceberg</name>
    <description>
        Iceberg connector plugin for Doris FE.
        Provides metadata operations for Iceberg tables via the Iceberg SDK Catalog API.
        Supports all Iceberg catalog backends: REST, HMS, Glue, DLF, JDBC, Hadoop, S3Tables.
        Phase 1 is metadata-only; scan planning, write operations, and transaction
        management remain in fe-core temporarily.
    </description>

    <dependencies>
        <dependency>
            <groupId>${project.groupId}</groupId>
            <artifactId>fe-connector-spi</artifactId>
            <version>${project.version}</version>
        </dependency>

        <!-- Iceberg per-engine metastore backends + the shared MetaStoreProvider SPI (D-059 Q2=B): the
             per-flavor property / HiveConf / DLF-conf assembly ported in P6-T05/T07 reuses the shared
             Abstract{Hms,Dlf}MetaStoreProperties conf (toHiveConfOverrides / toDlfCatalogConf) via
             bindForType("hms"/"dlf"), and P6-T10 wires validateProperties to bindForType(flavor).validate().
             Post P6-T10 the concrete iceberg impls + their ServiceLoader providers live in
             fe-connector-metastore-iceberg (mirroring fe-connector-metastore-paimon); it transitively pulls
             the extension point + framework + conf bases (metastore-spi), the contracts (metastore-api), and
             fe-kerberos. All bundled child-first into the plugin-zip, so only iceberg's providers are
             discoverable here (NOT paimon's). Hadoop/fs-free. -->
        <dependency>
            <groupId>${project.groupId}</groupId>
            <artifactId>fe-connector-metastore-iceberg</artifactId>
            <version>${project.version}</version>
        </dependency>

        <!-- Thrift types for constructing TTableFormatFileDesc / TIcebergFileDesc in populateRangeParams
             (P6.2-T03). provided scope: the host FE supplies fe-thrift at runtime, so it is NOT bundled into
             the plugin-zip (mirrors fe-connector-paimon); fe-connector-api's own fe-thrift is provided too
             and provided scope is non-transitive, hence the direct re-declaration. -->
        <dependency>
            <groupId>${project.groupId}</groupId>
            <artifactId>fe-thrift</artifactId>
            <version>${project.version}</version>
            <scope>provided</scope>
        </dependency>

        <!-- Iceberg SDK: catalog API, types, schema. Supplies the rest/hadoop/jdbc flavor catalog-impls
             (RESTCatalog / HadoopCatalog / JdbcCatalog), loaded by name via CatalogUtil. -->
        <dependency>
            <groupId>org.apache.iceberg</groupId>
            <artifactId>iceberg-core</artifactId>
            <version>${iceberg.version}</version>
        </dependency>

        <!-- Iceberg AWS integration: the glue flavor's org.apache.iceberg.aws.glue.GlueCatalog + S3FileIO.
             Needs the AWS SDK v2 modules below at runtime. -->
        <dependency>
            <groupId>org.apache.iceberg</groupId>
            <artifactId>iceberg-aws</artifactId>
            <version>${iceberg.version}</version>
        </dependency>

        <!-- Doris shaded Hive MetaStore client (D-059 Q1: DLF port-now). Supplies the hms flavor's
             org.apache.iceberg.hive.HiveCatalog AND the dlf flavor's reflectively-loaded
             com.aliyun.datalake.metastore.hive2.ProxyMetaStoreClient, both at their original package wired
             to a relocated thrift (shade.doris.hive.org.apache.thrift) so the old-package HMS client does
             NOT collide with the host fe-thrift 0.16. Same dependency fe-connector-hive/hms use; version
             managed (${doris.hive.catalog.shade.version}). plugin-zip.xml excludes fe-thrift/libthrift so
             no original-package thrift lands in the plugin (the relocated copy inside the shade is
             self-contained). -->
        <dependency>
            <groupId>org.apache.doris</groupId>
            <artifactId>hive-catalog-shade</artifactId>
        </dependency>

        <!-- Hadoop for HadoopCatalog and Configuration -->
        <dependency>
            <groupId>org.apache.hadoop</groupId>
            <artifactId>hadoop-common</artifactId>
            <version>${hadoop.version}</version>
        </dependency>

        <!-- AWS SDK v2 (BOM-managed, ${awssdk.version}) for the glue + s3tables + dlf-AWS paths. Bundled
             into the plugin so the connector's GlueCatalog / S3TablesCatalog get their OWN sdk-core
             ExecutionAttribute static under the child-first plugin loader; a second copy colliding with
             fe-core's permanently poisons S3 for the whole JVM (paimon RC-3). The set mirrors fe-core's
             iceberg AWS closure: glue/sts exclude apache-client (url-connection-client is the HTTP
             client); s3-transfer-manager carries the execution.interceptors SPI resource that must resolve
             child-first. software.amazon.awssdk MUST stay child-first (NOT in any parent-first allowlist). -->
        <dependency>
            <groupId>software.amazon.awssdk</groupId>
            <artifactId>s3</artifactId>
        </dependency>
        <dependency>
            <groupId>software.amazon.awssdk</groupId>
            <artifactId>glue</artifactId>
            <exclusions>
                <exclusion>
                    <groupId>software.amazon.awssdk</groupId>
                    <artifactId>apache-client</artifactId>
                </exclusion>
            </exclusions>
        </dependency>
        <dependency>
            <groupId>software.amazon.awssdk</groupId>
            <artifactId>sts</artifactId>
            <exclusions>
                <exclusion>
                    <groupId>software.amazon.awssdk</groupId>
                    <artifactId>apache-client</artifactId>
                </exclusion>
            </exclusions>
        </dependency>
        <dependency>
            <groupId>software.amazon.awssdk</groupId>
            <artifactId>s3tables</artifactId>
        </dependency>
        <dependency>
            <groupId>software.amazon.awssdk</groupId>
            <artifactId>s3-transfer-manager</artifactId>
        </dependency>
        <dependency>
            <groupId>software.amazon.awssdk</groupId>
            <artifactId>sdk-core</artifactId>
        </dependency>
        <dependency>
            <groupId>software.amazon.awssdk</groupId>
            <artifactId>aws-json-protocol</artifactId>
        </dependency>
        <dependency>
            <groupId>software.amazon.awssdk</groupId>
            <artifactId>protocol-core</artifactId>
        </dependency>
        <dependency>
            <groupId>software.amazon.awssdk</groupId>
            <artifactId>url-connection-client</artifactId>
        </dependency>

        <!-- S3Tables catalog impl (software.amazon.s3tables.iceberg.S3TablesCatalog) for the s3tables
             flavor. NOT in the AWS SDK BOM; version managed via dependencyManagement in fe/pom.xml
             (${s3tables.catalog.version}). -->
        <dependency>
            <groupId>software.amazon.s3tables</groupId>
            <artifactId>s3-tables-catalog-for-iceberg</artifactId>
        </dependency>

        <dependency>
            <groupId>org.apache.logging.log4j</groupId>
            <artifactId>log4j-api</artifactId>
        </dependency>

        <dependency>
            <groupId>org.junit.jupiter</groupId>
            <artifactId>junit-jupiter</artifactId>
            <scope>test</scope>
        </dependency>

        <!-- TEST ONLY: the assembleHiveConf parity test instantiates a real org.apache.hadoop.hive.conf.HiveConf,
             whose static init touches org.apache.hadoop.mapred.JobConf (hadoop-mapreduce-client-core). That
             artifact reaches the module only transitively (and is <optional> in hive-catalog-shade), so it is
             absent from the unit-test classpath. The PRODUCTION hms path runs inside the assembled plugin (full
             hadoop closure), so this is test-scope only — NOT bundled. Mirrors fe-connector-paimon. Version from
             dependencyManagement (${hadoop.version}). -->
        <dependency>
            <groupId>org.apache.hadoop</groupId>
            <artifactId>hadoop-mapreduce-client-core</artifactId>
            <scope>test</scope>
        </dependency>

        <!-- TEST ONLY: org.apache.hadoop.hive.conf.HiveConf static init references commons-lang 2.x
             (org.apache.commons.lang.StringUtils); like hadoop-mapreduce-client-core above it is present only
             transitively in the runtime plugin closure, not on the unit-test classpath. Test-scope only — NOT
             bundled. Version from dependencyManagement. -->
        <dependency>
            <groupId>commons-lang</groupId>
            <artifactId>commons-lang</artifactId>
            <scope>test</scope>
        </dependency>
    </dependencies>

    <build>
        <finalName>doris-fe-connector-iceberg</finalName>
        <plugins>
            <plugin>
                <artifactId>maven-assembly-plugin</artifactId>
                <configuration>
                    <appendAssemblyId>false</appendAssemblyId>
                    <descriptors>
                        <descriptor>src/main/assembly/plugin-zip.xml</descriptor>
                    </descriptors>
                </configuration>
                <executions>
                    <execution>
                        <id>make-assembly</id>
                        <phase>package</phase>
                        <goals>
                            <goal>single</goal>
                        </goals>
                    </execution>
                </executions>
            </plugin>
        </plugins>
    </build>
</project>

<?xml version="1.0" encoding="UTF-8"?>
<!--
Licensed to the Apache Software Foundation (ASF) under one
or more contributor license agreements.  See the NOTICE file
distributed with this work for additional information
regarding copyright ownership.  The ASF licenses this file
to you under the Apache License, Version 2.0 (the
"License"); you may not use this file except in compliance
with the License.  You may obtain a copy of the License at

  http://www.apache.org/licenses/LICENSE-2.0

Unless required by applicable law or agreed to in writing,
software distributed under the License is distributed on an
"AS IS" BASIS, WITHOUT WARRANTIES OR CONDITIONS OF ANY
KIND, either express or implied.  See the License for the
specific language governing permissions and limitations
under the License.
-->
<cache xmlns="http://maven.apache.org/BUILD-CACHE-CONFIG/1.2.0"
       xmlns:xsi="http://www.w3.org/2001/XMLSchema-instance"
       xsi:schemaLocation="http://maven.apache.org/BUILD-CACHE-CONFIG/1.2.0
                           https://maven.apache.org/xsd/build-cache-config-1.2.0.xsd">
    <configuration>
        <enabled>true</enabled>
        <hashAlgorithm>XX</hashAlgorithm>
    </configuration>
    <input>
        <global>
            <glob>{*.java,*.g4,*.xml,*.properties}</glob>
            <includes>
                <include>src/</include>
                <!-- Checkstyle configuration files affect all modules' style checks.
                     Include them globally so any change to checkstyle rules invalidates
                     all cached checkstyle results. -->
                <include glob="{*.xml,*.txt}" recursive="true">check/checkstyle</include>
                <!-- External source directories under gensrc/ are referenced by plugins via
                     Maven properties (e.g. ${project.parent.basedir}/../gensrc/thrift).
                     The cache extension's tagScanConfig cannot resolve these Maven properties,
                     so we must include these directories globally. This means all modules'
                     cache keys include these files. In practice, this causes some additional
                     cache invalidations for modules that are unaffected by gensrc changes, but
                     the overall impact on build performance has been evaluated as minor.
                     See: https://issues.apache.org/jira/browse/MBUILDCACHE-83 -->
                <include glob="*.thrift" recursive="true">../../gensrc/thrift</include>
                <include glob="*.proto" recursive="true">../../gensrc/proto</include>
                <include glob="{*.sh,*.py,Makefile}" recursive="true">../../gensrc/script</include>
            </includes>
        </global>
        <plugins>
            <plugin groupId="org.codehaus.mojo" artifactId="exec-maven-plugin">
                <dirScan>
                    <excludes>
                        <!-- DORIS_HOME points at the repository root in fe-core. Treating it as an
                             input directory pulls unrelated top-level files (for example local large
                             tarballs) into the checksum. Keep tracking env.sh and gensrc/script via
                             their dedicated tags/global includes, but skip this broad directory tag. -->
                        <exclude tagName="DORIS_HOME" />
                    </excludes>
                </dirScan>
            </plugin>
        </plugins>
    </input>
    <executionControl>
        <runAlways>
            <executions>
                <!-- Checkstyle is a quality gate that must always execute; never use a cached result. -->
                <execution artifactId="maven-checkstyle-plugin">
                    <execIds>
                        <execId>validate</execId>
                    </execIds>
                </execution>
                <!-- The wipe of target/lib that be-java-extensions/pom.xml binds right before the
                     copy-dependencies forced below. The two run as a pair or not at all: a copy
                     without the wipe deploys the union of every dependency set the directory has
                     ever held, which is the drift that wipe exists to prevent. -->
                <execution artifactId="maven-clean-plugin">
                    <execIds>
                        <execId>wipe-plugin-lib</execId>
                    </execIds>
                </execution>
            </executions>
            <goalsLists>
                <!-- A cache hit restores a module's jar and nothing else: whatever a skipped mojo
                     wrote elsewhere under target/ stays missing. Every BE Java plugin (and
                     hadoop-deps) deploys as a thin jar plus the runtime closure copy-dependencies
                     writes to target/lib, which build.sh copies verbatim, so a cache-restored
                     plugin would ship with no dependencies at all and fail the closure rule of
                     tools/be-java-plugins/check_plugin_layout.py. Harmless on a working copy, which
                     still holds the target/lib of its last real build; fatal in CI, which wipes
                     target/ before every build (git clean -fdx) while ~/.m2/build-cache survives -
                     the first build on an agent seeds the cache and every later one deploys empty
                     plugins. Forcing the goal only copies jars out of the local repository, far
                     cheaper than keeping the closure (hundreds of jars per plugin) in the cache as
                     attached outputs. -->
                <goalsList artifactId="maven-dependency-plugin">
                    <goals>
                        <goal>copy-dependencies</goal>
                    </goals>
                </goalsList>
            </goalsLists>
        </runAlways>
    </executionControl>
</cache>

<?xml version="1.0" encoding="UTF-8"?>
<!--
Licensed to the Apache Software Foundation (ASF) under one
or more contributor license agreements.  See the NOTICE file
distributed with this work for additional information
regarding copyright ownership.  The ASF licenses this file
to you under the Apache License, Version 2.0 (the
"License"); you may not use this file except in compliance
with the License.  You may obtain a copy of the License at

  http://www.apache.org/licenses/LICENSE-2.0

Unless required by applicable law or agreed to in writing,
software distributed under the License is distributed on an
"AS IS" BASIS, WITHOUT WARRANTIES OR CONDITIONS OF ANY
KIND, either express or implied.  See the License for the
specific language governing permissions and limitations
under the License.
-->
<cache xmlns="http://maven.apache.org/BUILD-CACHE-CONFIG/1.0.0"
       xmlns:xsi="http://www.w3.org/2001/XMLSchema-instance"
       xsi:schemaLocation="http://maven.apache.org/BUILD-CACHE-CONFIG/1.0.0
                           https://maven.apache.org/xsd/build-cache-config-1.0.0.xsd">
    <configuration>
        <enabled>true</enabled>
        <hashAlgorithm>XX</hashAlgorithm>
    </configuration>
    <input>
        <global>
            <glob>{*.java,*.g4,*.xml,*.properties}</glob>
            <includes>
                <include>src/</include>
                <!-- Checkstyle configuration files affect all modules' style checks.
                     Include them globally so any change to checkstyle rules invalidates
                     all cached checkstyle results. -->
                <include glob="{*.xml,*.txt}" recursive="true">check/checkstyle</include>
                <!-- External source directories under gensrc/ are referenced by plugins via
                     Maven properties (e.g. ${project.parent.basedir}/../gensrc/thrift).
                     The cache extension's tagScanConfig cannot resolve these Maven properties,
                     so we must include these directories globally. This means all modules'
                     cache keys include these files. In practice, this causes some additional
                     cache invalidations for modules that are unaffected by gensrc changes, but
                     the overall impact on build performance has been evaluated as minor.
                     See: https://issues.apache.org/jira/browse/MBUILDCACHE-83 -->
                <include glob="*.thrift" recursive="true">../../gensrc/thrift</include>
                <include glob="*.proto" recursive="true">../../gensrc/proto</include>
                <include glob="{*.sh,*.py,Makefile}" recursive="true">../../gensrc/script</include>
            </includes>
        </global>
        <plugins>
            <plugin groupId="org.codehaus.mojo" artifactId="exec-maven-plugin">
                <dirScan>
                    <excludes>
                        <!-- DORIS_HOME points at the repository root in fe-core. Treating it as an
                             input directory pulls unrelated top-level files (for example local large
                             tarballs) into the checksum. Keep tracking env.sh and gensrc/script via
                             their dedicated tags/global includes, but skip this broad directory tag. -->
                        <exclude tagName="DORIS_HOME" />
                    </excludes>
                </dirScan>
            </plugin>
        </plugins>
    </input>
    <executionControl>
        <runAlways>
            <!-- Checkstyle is a quality gate that must always execute; never use a cached result. -->
            <executions>
                <execution artifactId="maven-checkstyle-plugin">
                    <execIds>
                        <execId>validate</execId>
                    </execIds>
                </execution>
                <!-- dependency:copy-dependencies writes JARs to target/lib/ which the build
                     scripts later copy into the output directory. The cache extension only
                     restores the primary JAR artifact on a hit, not the target/lib/ tree.
                     When combined with a clean build, target/lib/ is
                     deleted by the clean phase and never recreated, causing "cp: cannot stat
                     target/lib/*" failures. Force this goal to always run so that the lib
                     directory is always populated regardless of cache state. -->
                <execution artifactId="maven-dependency-plugin">
                    <execIds>
                        <execId>copy-dependencies</execId>
                    </execIds>
                </execution>
            </executions>
        </runAlways>
    </executionControl>
</cache>

<?xml version="1.0" encoding="UTF-8"?>
<!--
Licensed to the Apache Software Foundation (ASF) under one
or more contributor license agreements.  See the NOTICE file
distributed with this work for additional information
regarding copyright ownership.  The ASF licenses this file
to you under the Apache License, Version 2.0 (the
"License"); you may not use this file except in compliance
with the License.  You may obtain a copy of the License at

  http://www.apache.org/licenses/LICENSE-2.0

Unless required by applicable law or agreed to in writing,
software distributed under the License is distributed on an
"AS IS" BASIS, WITHOUT WARRANTIES OR CONDITIONS OF ANY
KIND, either express or implied.  See the License for the
specific language governing permissions and limitations
under the License.
-->
<project xmlns:xsi="http://www.w3.org/2001/XMLSchema-instance" xmlns="http://maven.apache.org/POM/4.0.0"
         xsi:schemaLocation="http://maven.apache.org/POM/4.0.0 http://maven.apache.org/xsd/maven-4.0.0.xsd">
    <modelVersion>4.0.0</modelVersion>
    <parent>
        <groupId>org.apache.doris</groupId>
        <version>${revision}</version>
        <artifactId>fe</artifactId>
        <relativePath>../pom.xml</relativePath>
    </parent>
    <properties>
        <!-- project.basedir (not project.parent.basedir): the latter is not interpolated
             at model-build time, so its raw value would leak into the antrun check below. -->
        <doris.thrift.executable>${project.basedir}/../../thirdparty/installed/bin/thrift</doris.thrift.executable>
        <doris.thrift.source>${project.basedir}/../../gensrc/thrift</doris.thrift.source>
    </properties>
    <profiles>
        <profile>
            <id>thrift-executable-override</id>
            <activation>
                <property>
                    <name>env.DORIS_THIRDPARTY</name>
                </property>
            </activation>
            <properties>
                <doris.thrift.executable>${env.DORIS_THIRDPARTY}/installed/bin/thrift</doris.thrift.executable>
            </properties>
        </profile>
        <profile>
            <id>release</id>
            <build>
                <plugins>
                    <plugin>
                        <groupId>org.apache.maven.plugins</groupId>
                        <artifactId>maven-source-plugin</artifactId>
                        <configuration>
                            <attach>true</attach>
                        </configuration>
                        <executions>
                            <execution>
                                <id>create-source-jar</id>
                                <goals>
                                    <goal>jar-no-fork</goal>
                                </goals>
                            </execution>
                        </executions>
                    </plugin>
                </plugins>
            </build>
        </profile>
    </profiles>
    <artifactId>fe-thrift</artifactId>
    <packaging>jar</packaging>
    <name>Doris FE Generated Thrift RPC</name>
    <description>Generated Thrift RPC for Doris Frontend modules</description>

    <dependencies>
        <!-- https://mvnrepository.com/artifact/org.apache.thrift/libthrift -->
        <dependency>
            <groupId>org.apache.thrift</groupId>
            <artifactId>libthrift</artifactId>
        </dependency>
    </dependencies>

    <build>
        <finalName>doris-fe-thrift</finalName>
        <directory>${project.basedir}/target/</directory>
        <plugins>
            <plugin>
                <!-- Generated code only compiles against the matching libthrift; fail fast
                     with a clear message instead of hundreds of generic-arity errors. -->
                <groupId>org.apache.maven.plugins</groupId>
                <artifactId>maven-antrun-plugin</artifactId>
                <version>3.1.0</version>
                <executions>
                    <execution>
                        <id>check-thrift-compiler-version</id>
                        <phase>initialize</phase>
                        <goals>
                            <goal>run</goal>
                        </goals>
                        <configuration>
                            <target>
                                <exec executable="${doris.thrift.executable}" outputproperty="thrift.compiler.version" failonerror="true">
                                    <arg value="--version"/>
                                </exec>
                                <fail message="thrift compiler '${doris.thrift.executable}' reports '${thrift.compiler.version}' but libthrift is ${thrift.version}. Rebuild thirdparty, or point doris.thrift.executable / DORIS_THIRDPARTY at a thrift ${thrift.version} binary.">
                                    <condition>
                                        <not>
                                            <contains string="${thrift.compiler.version}" substring="${thrift.version}"/>
                                        </not>
                                    </condition>
                                </fail>
                            </target>
                        </configuration>
                    </execution>
                </executions>
            </plugin>
            <plugin>
                <groupId>org.apache.thrift</groupId>
                <artifactId>thrift-maven-plugin</artifactId>
                <version>0.10.0</version>
                <configuration>
                    <generator>java:fullcamel,generated_annotations=suppress</generator>
                    <thriftExecutable>${doris.thrift.executable}</thriftExecutable>
                    <thriftSourceRoot>${doris.thrift.source}</thriftSourceRoot>
                </configuration>
                <executions>
                    <execution>
                        <id>thrift-sources</id>
                        <phase>generate-sources</phase>
                        <goals>
                            <goal>compile</goal>
                        </goals>
                    </execution>
                </executions>
            </plugin>
        </plugins>
    </build>
</project>

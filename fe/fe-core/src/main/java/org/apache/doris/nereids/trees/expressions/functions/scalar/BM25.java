// Licensed to the Apache Software Foundation (ASF) under one
// or more contributor license agreements.  See the NOTICE file
// distributed with this work for additional information
// regarding copyright ownership.  The ASF licenses this file
// to you under the Apache License, Version 2.0 (the
// "License"); you may not use this file except in compliance
// with the License.  You may obtain a copy of the License at
//
//   http://www.apache.org/licenses/LICENSE-2.0
//
// Unless required by applicable law or agreed to in writing,
// software distributed under the License is distributed on an
// "AS IS" BASIS, WITHOUT WARRANTIES OR CONDITIONS OF ANY
// KIND, either express or implied.  See the License for the
// specific language governing permissions and limitations
// under the License.

package org.apache.doris.nereids.trees.expressions.functions.scalar;

import org.apache.doris.catalog.FunctionSignature;
import org.apache.doris.nereids.exceptions.AnalysisException;
import org.apache.doris.nereids.trees.expressions.Expression;
import org.apache.doris.nereids.trees.expressions.functions.ExplicitlyCastableSignature;
import org.apache.doris.nereids.trees.expressions.functions.Nondeterministic;
import org.apache.doris.nereids.trees.expressions.visitor.ExpressionVisitor;
import org.apache.doris.nereids.types.FloatType;
import org.apache.doris.nereids.util.ExpressionUtils;

import com.google.common.base.Preconditions;
import com.google.common.collect.ImmutableList;

import java.util.List;

/**
 * ScalarFunction 'random'. This class is generated by GenerateFunction.
 */
public class BM25 extends ScalarFunction
        implements ExplicitlyCastableSignature, Nondeterministic {

    public static final List<FunctionSignature> SIGNATURES = ImmutableList.of(
            FunctionSignature.ret(FloatType.INSTANCE).args(),
            FunctionSignature.ret(FloatType.INSTANCE).args(FloatType.INSTANCE)
    );

    /**
     * Constructor with no arguments.
     */
    public BM25() {
        super("bm25");
    }

    /**
     * Constructor with one argument, used as a push-down function created by the optimizer for the storage engine.
     */
    public BM25(Expression arg) {
        super("bm25", arg);
        Preconditions.checkState(ExpressionUtils.isVirtualProjCol(arg),
                "Expected a virtual projection column, but got %s", arg);
    }

    /**
     * custom compute nullable.
     */
    @Override
    public boolean nullable() {
        return false;
    }

    @Override
    public boolean foldable() {
        return false;
    }

    /**
     * withChildren.
     */
    @Override
    public BM25 withChildren(List<Expression> children) {
        if (children.isEmpty()) {
            return new BM25();
        } else if (children.size() == 1) {
            return new BM25(children.get(0));
        }

        throw new AnalysisException("BM25 function only accept 0 or 1 argument");
    }

    @Override
    public List<FunctionSignature> getSignatures() {
        return SIGNATURES;
    }

    @Override
    public <R, C> R accept(ExpressionVisitor<R, C> visitor, C context) {
        return visitor.visitBM25(this, context);
    }
}

// Licensed to the Apache Software Foundation (ASF) under one
// or more contributor license agreements.  See the NOTICE file
// distributed with this work for additional information
// regarding copyright ownership.  The ASF licenses this file
// to you under the Apache License, Version 2.0 (the
// "License"); you may not use this file except in compliance
// with the License.  You may obtain a copy of the License at
//
//   http://www.apache.org/licenses/LICENSE-2.0
//
// Unless required by applicable law or agreed to in writing,
// software distributed under the License is distributed on an
// "AS IS" BASIS, WITHOUT WARRANTIES OR CONDITIONS OF ANY
// KIND, either express or implied.  See the License for the
// specific language governing permissions and limitations
// under the License.

package org.apache.doris.nereids.trees.expressions.functions.agg;

import org.apache.doris.catalog.FunctionSignature;
import org.apache.doris.nereids.exceptions.AnalysisException;
import org.apache.doris.nereids.trees.expressions.Expression;
import org.apache.doris.nereids.trees.expressions.functions.ExplicitlyCastableSignature;
import org.apache.doris.nereids.trees.expressions.literal.Literal;
import org.apache.doris.nereids.trees.expressions.visitor.ExpressionVisitor;
import org.apache.doris.nereids.types.DoubleType;

import com.google.common.base.Preconditions;
import com.google.common.collect.ImmutableList;

import java.util.List;

/**
 * AggregateFunction 'percentile_approx'. This class is generated by GenerateFunction.
 */
public class PercentileApprox extends NullableAggregateFunction
        implements ExplicitlyCastableSignature {

    public static final List<FunctionSignature> SIGNATURES = ImmutableList.of(
            FunctionSignature.ret(DoubleType.INSTANCE).args(DoubleType.INSTANCE, DoubleType.INSTANCE),
            FunctionSignature.ret(DoubleType.INSTANCE)
                    .args(DoubleType.INSTANCE, DoubleType.INSTANCE, DoubleType.INSTANCE)
    );

    /**
     * constructor with 2 arguments.
     */
    public PercentileApprox(Expression arg0, Expression arg1) {
        this(false, arg0, arg1);
    }

    /**
     * constructor with 2 arguments.
     */
    public PercentileApprox(boolean distinct, Expression arg0, Expression arg1) {
        this(distinct, false, arg0, arg1);
    }

    public PercentileApprox(boolean distinct, boolean alwaysNullable, Expression arg0, Expression arg1) {
        super("percentile_approx", distinct, alwaysNullable, arg0, arg1);
    }

    /**
     * constructor with 3 arguments.
     */
    public PercentileApprox(Expression arg0, Expression arg1, Expression arg2) {
        this(false, arg0, arg1, arg2);
    }

    /**
     * constructor with 3 arguments.
     */
    public PercentileApprox(boolean distinct, Expression arg0, Expression arg1, Expression arg2) {
        this(distinct, false, arg0, arg1, arg2);
    }

    public PercentileApprox(boolean distinct, boolean alwaysNullable, Expression arg0,
            Expression arg1, Expression arg2) {
        super("percentile_approx", distinct, alwaysNullable, arg0, arg1, arg2);
    }

    /** constructor for withChildren and reuse signature */
    private PercentileApprox(NullableAggregateFunctionParams functionParams) {
        super(functionParams);
    }

    @Override
    public void checkLegalityBeforeTypeCoercion() {
        Expression quantile = getArgument(1);
        if (!quantile.isConstant()) {
            throw new AnalysisException(
                    "percentile_approx requires second parameter must be a constant : " + this.toSql());
        }
        if (arity() == 3) {
            Expression compressionArg = getArgument(2);
            if (!compressionArg.isConstant()) {
                throw new AnalysisException(
                        "percentile_approx requires the third parameter must be a constant : " + this.toSql());
            }
        }
    }

    @Override
    public void checkLegalityAfterRewrite() {
        checkLegalityBeforeTypeCoercion();
        Expression quantile = getArgument(1);
        if (!(quantile instanceof Literal) || !quantile.getDataType().isNumericType()) {
            return;
        }
        double value = ((Literal) quantile).getDouble();
        if (value < 0.0 || value > 1.0) {
            throw new AnalysisException(
                    "percentile_approx quantile must be in [0, 1], but got " + value + ": " + this.toSql());
        }
    }

    /**
     * withDistinctAndChildren.
     */
    @Override
    public PercentileApprox withDistinctAndChildren(boolean distinct, List<Expression> children) {
        Preconditions.checkArgument(children.size() == 2
                || children.size() == 3);
        return new PercentileApprox(getFunctionParams(distinct, children));
    }

    @Override
    public PercentileApprox withAlwaysNullable(boolean alwaysNullable) {
        return new PercentileApprox(getAlwaysNullableFunctionParams(alwaysNullable));
    }

    @Override
    public <R, C> R accept(ExpressionVisitor<R, C> visitor, C context) {
        return visitor.visitPercentileApprox(this, context);
    }

    @Override
    public List<FunctionSignature> getSignatures() {
        return SIGNATURES;
    }
}

# 🤝 交接文档 · 连接器公共接口整治

> **滚动文档**：每轮结束后**覆盖式更新**，只保留下一个 session 必须的上下文。已完成工作的明细不落这里（在 `git log` 与各任务文档里）。
> **范围** = 把 `fe-connector-api` / `fe-connector-spi` 两个公共模块的接口设计规范化。
> **⚠ 与主线互不覆盖**：catalog SPI 迁移主线的交接文档是 `plan-doc/HANDOFF.md`（当前跟的是另一条线）。**不要用本文覆盖它，也不要用它覆盖本文。**

---

## 🆕 下一个 session 起步

**必读顺序**：本文 → [README.md](./README.md) 的任务清单 → 挑中的那个任务自己的文档。
**不要通读** `audit-report.md`（1600 余行），按 README 里的章节导航 grep 定位。

**当前状态：第一批已合入（5 个提交），代码已开始改动。**

已完成：07（写下规则）、08（文档据实）、10（按域拆接口），外加调研期发现的**两个真实用户可见缺口**已修（异构 HMS 目录下嵌套列 DDL 打不通、iceberg 表注释显示为空）。

**建议的下一步**：**11**（删第一批死接口面，纯公共模块、不动连接器）。理由：10 号已经把域边界和「最少实现集」写下来了，删除批次现在有判据；而且 11 号是 15 号（删目录类型白名单，本线核心）的前置。

**做 11 号之前必看两条**（本轮实测订正，写在任务文档里的旧结论有误）：

1. **`getRangeType()` 不是死方法**。fe-core 确实从不读它，但 `fe-connector-api` 自己的 `ConnectorScanRange.populateRangeParams` 默认实现读它，并把名字发进 BE 可见的 jdbc 参数里；jdbc 是唯一走这个默认实现的连接器。**删除分片类型枚举族（13 号）会改变 jdbc 发给 BE 的内容**，必须按行为改动处理并配回归，不能按「删死代码」处理。
2. **新增了一道减速带**：`ConnectorMetadataSurfaceTest` 把 `ConnectorMetadata` 的 81 条方法签名冻结在 `fe-connector-api/src/test/resources/connector-metadata-methods.txt`。任何删除 SPI 方法的批次**必须在同一个提交里重新生成这份基线**，否则测试变红。这是故意的。

---

## 📍 三句话交代这条线在解决什么

1. **「新增连接器不需要修改公共模块」这句话今天在代码上是假的。** `CatalogFactory` 里有一个写死的七元素类型白名单，第三方连接器即使正确注册并成功装配，`CREATE CATALOG` 也不会走到它。这是 15 号任务，**仍未做**。
2. ~~新连接器作者无处可依。~~ **已解决**：两个模块各有一份包级说明，写下了能力声明分层、异常族、thrift 边界、模块划分、生命周期与线程模型五条规则；`ConnectorTableOps` 已按域拆成 6 个父接口，每域的类文档写清「最少实现集」，并有注解 + 单测钉住。
3. ~~接口文档与实现大面积脱节。~~ **已解决**（08 号那九处）。剩余的文档问题在 09 号（下推表达式契约）。

---

## 🔑 动手前必须知道的五件事

1. **行号信内容不信文档。** 全部文档的行号以 `7ff51a106f0` 为准；本轮已确认那个提交与本轮起点在两个公共模块上**逐字节相同**，但 10 号落地后 `ConnectorTableOps` 已从 504 行缩到 69 行、46 个方法搬进 6 个新文件，**该文件的所有行号已作废**。核对一律以符号名为准。
2. **先读调研报告第十四节（被推翻或收窄的说法）和第十六节（明确不建议动的部分）。** 误报比漏报更毒。
3. **删除类任务必须全仓复扫**，且统计连接器实现分布时**按符号 grep，不要按类名模式**（trino 的元数据类叫 `TrinoConnectorDorisMetadata`，按 `*ConnectorMetadata.java` 统计会静默漏掉它）。
4. **`fe-core` 只出不进。**
5. **两个连接器的类加载器钉桩包装类是雷区**（06 号任务；23 号依赖它先做）。

---

## ⚙️ 构建与验证的坑（实测，直接复用，别再踩）

1. **全反应堆 `test-compile` 必须排除两个 shade 模块**，否则 hive 相关模块必然编译失败（`package org.apache.hadoop.hive.conf does not exist`）。原因：那两个模块是 shade 重打包模块，反应堆内会用它们**未 shade 的 `target/classes`** 去解析依赖，而不是本地仓库里已安装的 shaded jar。**这与你的改动无关，不要去 debug 它**：
   ```
   mvn -o -f /mnt/disk1/yy/git/wt-catalog-spi/fe/pom.xml \
     -pl '!fe-connector/fe-connector-hms-hive-shade,!fe-connector/fe-connector-paimon-hive-shade' \
     -Dmaven.build.cache.enabled=false -T1C test-compile
   ```
2. **maven build cache 会静默跳过测试执行**：所有跑测试的命令都要加 `-Dmaven.build.cache.enabled=false`。注意 `fe/.mvn/maven.config` 是**未跟踪文件**（只在本工作树存在），不要依赖它。
3. **`mvn ... | tail` 之后的 `$?` 是 `tail` 的**；读 `BUILD SUCCESS` / `BUILD FAILURE` 行。
4. **maven 一律用绝对路径 `-f`**（跨工具调用工作目录会持久，`cd` 会破坏相对路径——本轮又踩了一次）。
5. **`-Dtest='org.apache.doris.datasource.**'` 这种全包扫描会超时被砍**，用具体类名清单。
6. **严禁 `git add -A`**（工作树有大量历史遗留临时文件与含明文密钥的配置），一律 path-whitelist。
7. **e2e（groovy）需要真集群，本地跑不了**。
8. **`HiveConnectorMetadataDdlTest` 在本分支上本来就是红的**（5 failures + 7 errors，建表路径），与本线改动无关——已用 `git stash` 对照确认。**别把它当成自己改坏了**，也别顺手修（属另一条线）。

---

## 📦 提交规范

- **每个任务一个独立 commit**；任务文档 / 交接文档与代码**分开提交**。
- 提交信息**全英文**，标题形如 `[refactor](catalog) fe-connector-api: <what changed>`。
- 结尾附 `Co-Authored-By:` 与 `Claude-Session:` 两行。
- `gh pr edit` 在上游仓库上是坏的，改标题/正文用 REST API。

---

## 🧭 待用户拍板（未决之前不要顺手做掉）

完整清单在 **[open-decisions.md](./open-decisions.md)**。**本轮已拍板三条**：

1. **含隐式类型转换的谓词下推默认值**：**只改文档，默认值不动**。翻转另开一项，需配逐连接器判断与端到端回归。
2. **最少实现集的形式**：**注解 + 冻结测试**（已落地）。
3. **调研期发现的两个真实缺口**：**本轮顺手修掉**（已落地，e2e 待有集群时执行）。

**仍待拍板**：
- **连接器自声明属性：接线还是删除**（24 号，文档推荐删除）。
- **建表能力下沉的报错文案变化**（18 号；同一文档还纠正了「仓库里根本不存在分桶子句的正向端到端护栏」）。
- **插件与内建目录类型的路由优先级**（15 号，文档采纳「插件优先」）。
- 11 号里的两条判断题（建表请求的「是否外部表」字段、读侧的主键方法）：建议删；若保留必须同时补契约文档并让至少一个连接器真正消费。

---

## 📈 进度记录

| 日期 | 做了什么 | 结果 |
|---|---|---|
| 2026-07-25 | 独立 clean-room 调研（14 个并行审查单元 + 30 批对抗复核） | 172 条结论成立/部分成立，4 条被推翻；产出 `audit-report.md` |
| 2026-07-25 | 建立本任务空间，按优先级拆出 25 个任务并各写一份施工文档 | 代码零改动 |
| 2026-07-25 | 第一批落地：07 + 08 + 10 三个提交；动手前用 11 个 agent 复核三份文档的全部事实断言，推翻/收窄 9 条 | 全反应堆含测试源 `test-compile` 通过；`fe-connector-api` 79 个单测通过；冻结测试双变异验证均变红 |
| 2026-07-25 | 修掉调研期发现的两个用户可见缺口（异构目录嵌套列 DDL、iceberg 表注释），补单测与 e2e 用例 | 单测通过；e2e 已写出但**未执行**（需真集群） |

**上下文用量超过 30% 就找一个干净节点覆写本文并通知用户开新 session 续做**，不要等窗口满。

input {
    stdin {
        codec => "line"
    }
}

output {
    doris {
        http_hosts => ["http://${DORIS_HOST}:${DORIS_FE_PORT}"]
        user => "${DORIS_USER}"
        password => "${DORIS_PASSWORD}"
        db => "logstash_db"
        table => "csv_enclose"
        message_only => true
        headers => {
          "format" => "csv"
          "column_separator" => ","
          "trim_double_quotes" => "true"
          "enclose" => '"'
        }
    }
}

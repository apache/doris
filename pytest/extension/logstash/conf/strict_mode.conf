input {
    stdin {
        codec => "json"
    }
}

output {
    doris {
        http_hosts => ["http://localhost:8030"]
        user => "admin"
        password => "admin"
        db => "logstash_db"
        table => "strict_mode"
        headers => {
          "format" => "json"
          "read_json_by_line" => "true"
          "max_filter_ratio" => "0.5"
          "strict_mode" => "true"
        }
    }
}

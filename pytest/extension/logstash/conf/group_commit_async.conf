input {
    stdin {
        codec => "json"
    }
}

output {
    doris {
        http_hosts => ["http://localhost:8030"]
        user => "admin"
        password => "admin"
        db => "logstash_db"
        table => "group_commit_async"
        headers => {
          "format" => "json"
          "read_json_by_line" => "true"
          "group_commit" => "async_mode"
        }
    }
}
